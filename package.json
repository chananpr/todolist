{
  "name": "taskforge-enterprise",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "dev": "npm run dev:api & npm run dev:web",
    "build": "npm run build -w @taskforge/contracts && npm run build -w apps/api && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w @taskforge/contracts && npm run typecheck -w apps/api && npm run typecheck -w apps/web"
  }
}
